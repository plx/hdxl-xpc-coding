// swift-tools-version: 6.3

import PackageDescription

let package = Package(
  name: "IndependentXPCCodingAdversarial",
  platforms: [
    .macOS(.v26)
  ],
  dependencies: [
    .package(
      name: "hdxl-xpc-coding",
      path: "/tmp/hdxl-xpc-audit52.UvYJGp/candidate"
    )
  ],
  targets: [
    .executableTarget(
      name: "IndependentXPCCodingAdversarial",
      dependencies: [
        .product(
          name: "XPCCoding",
          package: "hdxl-xpc-coding"
        )
      ],
      swiftSettings: [
        .enableExperimentalFeature("StrictConcurrency")
      ]
    )
  ],
  swiftLanguageModes: [.v6]
)
